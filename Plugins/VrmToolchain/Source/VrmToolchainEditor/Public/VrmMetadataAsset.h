#pragma once

// Re-export the runtime metadata asset class and types for editor use.
// This maintains backward compatibility while ensuring the type is defined in runtime.
#include "VrmToolchain/Public/VrmMetadataAsset.h"
